[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ebook2md"
version = "0.2.0"
description = "Local-first ebook and document conversion to auditable Markdown"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
  "beautifulsoup4>=4.13.0,<5",
  "huggingface-hub>=0.34.0,<2",
  "mdformat==1.0.0",
  "mdformat-gfm==1.0.0",
  "pillow>=11.0.0,<13",
  "pymarkdownlnt==0.9.38",
  "pymupdf>=1.26.0,<2",
]

[project.optional-dependencies]
ocr = [
  "mlx-vlm @ git+https://github.com/Blaizzy/mlx-vlm.git@fbdfc837da0ee197a18859ea327ede858631bdb1",
]
dev = [
  "pytest>=8.4.0,<10",
]

[project.scripts]
ebook2md = "ebook2md.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ebook2md"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
