[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "speech2md"
version = "0.1.0"
description = "Local MOSS transcription with ReDimNet2 speaker reconciliation to Markdown"
readme = "README.md"
requires-python = ">=3.11,<3.15"
license = "MIT"
dependencies = [
  "mlx-audio[stt]==0.4.6",
  "numpy>=2,<3",
  "pyyaml>=6,<7",
  "scipy>=1.15,<2",
  "torch==2.11.0",
  "torchaudio==2.11.0",
  "tqdm>=4.67,<5",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4,<10",
]

[project.scripts]
speech2md = "speech2md.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/speech2md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
